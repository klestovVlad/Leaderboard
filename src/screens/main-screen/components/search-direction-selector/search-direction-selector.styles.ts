import {StyleSheet} from 'react-native';

export const themedStyles = StyleSheet.create({
  root: {
    marginTop: 16,
    gap: 16,
    flexDirection: 'row',
    width: '100%',
    justifyContent: 'center',
  },
});
