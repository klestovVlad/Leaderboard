import {StyleSheet} from 'react-native';

export const themedStyles = StyleSheet.create({
  root: {
    flexGrow: 1,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
